# 動機と背景(NOTE.md)

## 背景
AWSリソース管理において、既存ツールには以下の課題が存在しました：
1. **アクセスキーの長期利用**:
   - IAMユーザーのアクセスキーを長期間有効化すると、セキュリティリスクが高まる。
   - 例えば、アクセスキーが漏洩すると、権限管理が不十分な環境では不正アクセスが容易になります。
2. **クロスアカウントアクセスの複雑さ**:
   - 例えば、運用において複数アカウントのリソースを横断的に管理する場合、各アカウントでの手動設定が煩雑になりやすい。
3. **設定や操作の学習コストが高い**:
   - AWS CLIやSDKのコマンドにはオプションが多く、初心者には使いこなすまで時間がかかります。
   - シンプルにリソース管理を行いたい場合でも、これらのツールでは過剰機能が多い場合があります。

## 動機
**awstidy**は、以下の目標を達成するために設計されました：

### ユースケース例
- **スタートアップ企業**: 限られたリソースで効率的にAWSを管理したいが、専門知識が十分でないエンジニアが中心となって運用を行っている。
- **中規模チーム**: 開発・運用チームのメンバーが複数いる環境で、権限管理やリソース整理が後手に回り、全体のセキュリティリスクが増大している。
- **個人開発者**: AWSを趣味や学習目的で利用しているが、リソースの削除漏れや整理不足でコストが増加してしまう懸念がある。

### 具体的な機能目標
1. **セキュリティ向上**:
   - AWS STSを使用した一時認証を採用し、アクセスキーを利用しないセキュアな方法を提供。
   - CLIコマンド経由での操作のみで、ブラウザを介した操作は不要。
     - CLIは対象AWSアカウントの最小限に設定された権限セット(STS)で動作する。
   - アクセスキーの利用はawstidyの初期設定時のみで、その後は利用しない。

2. **設定の自動化**:
   - 必要最低限のIAMロール・ポリシーをIaC（AWS CDKを想定）により自動デプロイ。

3. **直感的な操作**:
   - シンプルなCLIコマンドで、リソースの一覧取得、整理、削除を可能に。
   - INPUTは最低限かつシンプルなものにし、OUTPUTはCSV、JSON、テキストなど汎用フォーマットをサポート。
     - 例: `awstidy list-resources` で一括取得、`awstidy delete-resource` で不要リソースを簡単に削除可能。

4. **初心者に優しい設計**:
   - シンプルなコマンド構成で、学習コストを最小限に抑える。
   - リソース整理・削除など、実行前に確認を求める設計で誤操作を防止。
