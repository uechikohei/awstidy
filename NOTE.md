# 動機と背景

## 背景
AWSリソース管理において、既存ツールには以下の課題が存在しました：
1. **アクセスキーの長期利用**:
   - IAMユーザーのアクセスキーを長期間有効化すると、セキュリティリスクが高まる。
2. **クロスアカウントアクセスの複雑さ**:
   - 設定作業が煩雑で初心者にとってはハードルが高い。
3. **設定や操作の学習コストが高い**:
   - 利用までの手間や設定が多く、シンプルにAWSリソースを管理したい場合に適さない。

### 参考ツール
以下は既存ツールの例で、それぞれの特徴や利用方法が異なりますが、課題を解決するための設計思想に影響を与えました：
- **[AWSweeper](https://github.com/jckuester/awsweeper)**:
    - AWSアカウント内のリソースをクリーンアップするツール。290以上のリソースタイプに対応し、YAMLファイルでフィルタを設定してリソースの削除を行います。
    - モジュール化構成（APP、API、WEB）を採用。
- **[aws-auto-cleanup](https://github.com/mlevit/aws-auto-cleanup)**:
    - ユーザー定義の許可リストとTTL（Time To Live）設定に基づいて、不要なAWSリソースを自動削除。モジュール化構成（APP、API、WEB）を採用。
- **[aws-account-cleanup](https://github.com/HemanthGangula/aws-account-cleanup)**:
    - EC2、S3、Lambda、IAMなどのAWSリソースを対象に、Pythonスクリプトでクリーンアップを自動化。インラインポリシー管理もサポート。
- **[AWS Resource Tracker](https://github.com/pranjalraut03/AWS-Resource-Tracker)**:
    - BashスクリプトとAWS CLIを使用して、AWSリソースの追跡と監視を行います。EC2、S3、IAM、RDSなどに対応。
- **[AWS CLI Management Tool](https://github.com/infosecsingh/AWS-CLI-Management-Tool)**:
    - PythonベースのCLIツールで、リソースの取得、作成、削除、監視を簡素化。S3バケットやLambda関数の管理に対応。

---

## 動機
**awstidy**は、既存ツールの課題を解決し、以下の目標を達成するために設計されました：

1. **セキュリティ向上**:
   - AWS STSを使用した一時認証を採用し、アクセスキーを利用しないセキュアな方法を提供。
   - アクセスキーは初期設定時（`setup-iam`実行）にのみ使用し、その後は利用しません。
   - CLIコマンド経由の操作に限定し、ブラウザ操作を不要に。
   - 各CLI操作は、対象AWSアカウントにおける最小権限セット（IAMロール＋STS）で動作。

2. **設定の自動化**:
   - 必要最低限のIAMロールやポリシーを、IaC（AWS CDKを想定）で自動デプロイ。
   - ユーザーの手動設定の負担を最小限に抑える。

3. **直感的な操作**:
   - シンプルなCLIコマンドで、リソースの一覧取得、整理、削除を可能に。
   - **入力（INPUT）**: 必要最低限のパラメータのみで操作を完了。
   - **出力（OUTPUT）**: CSV、JSON、テキストなど、汎用的なフォーマットに対応。

4. **初心者に優しい設計**:
   - 初心者でもAWSリソースを簡単に管理できる設計。
   - CLI操作の手順を分かりやすくドキュメント化。

---

## 今後の展望
1. 他のクラウドプロバイダー（GCP、Azure、Oracle Cloudなど）への対応。
2. AWSリソースの利用状況やコスト情報をさらに詳細に整理する機能の追加。
3. 大規模なAWS環境や複数アカウントでの利用に向けた最適化。
4. ユーザーからのフィードバックを元に、さらなる操作性向上を目指す。
